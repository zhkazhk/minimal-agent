# 运行期依赖：无。本项目只用 Python 标准库（urllib / asyncio / json / ast / dataclasses）。
# 直接 `python -m miniagent demo` 即可跑通全部用例，不需要 pip install 任何东西。

# ---- 可选增强（不装也能跑）----
# httpx>=0.27        # MINIAGENT_HTTP_BACKEND=httpx 时使用原生 async HTTP
# aiohttp>=3.9       # MINIAGENT_HTTP_BACKEND=aiohttp
# jsonschema>=4.20   # 工具参数校验改用官方 JSON Schema 实现（更严格）

# ---- 开发/测试 ----
pytest>=7.4
